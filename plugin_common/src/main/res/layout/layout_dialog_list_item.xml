<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/SmallListItemHeight"
    android:background="@drawable/list_item_selector_no_divider"
    android:gravity="center_vertical"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="@dimen/LittleDialogPadding"
        android:paddingRight="@dimen/LittleDialogPadding">

        <ImageView
            android:id="@id/icon"
            android:layout_width="@dimen/MiniAvatarSize"
            android:layout_height="@dimen/MiniAvatarSize"
            android:layout_marginRight="@dimen/NormalPadding" />

        <TextView
            android:id="@id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/base_text_dark_color"
            android:textSize="@dimen/NormalTextSize" />
    </LinearLayout>

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="1.0dip"
        android:background="@color/list_divider" />
</LinearLayout>